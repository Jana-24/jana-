#include <stdio.h>
int main()
{
	int a,b,c=1;
	printf("enter a number to find a factorial: ");
	scanf("%d",&a);
	for(b=1;b<=a;b++){
		c=c*b;
	}
	printf("factorial of number %d is %d.",a,c);
	return 0;
}
